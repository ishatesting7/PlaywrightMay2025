{
  "name": "playwrightmay2025",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "runTest": "npx playwright test",
    "runTestHeadless": "npx playwright test --headed",
    "runTestWithRegression": "npx playwright test --grep '@regression'",
    "runTestWithSmoke": "npx playwright test --grep '@smoke'",
    "runTestWithoutTagRegression": "npx playwright test --grep-invert '@regression'",
    "openLatestHTMLReport": "npx playwright show-report",
    "runFailedTestCases": "npx playwright test --last-failed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/node": "^22.15.17",
    "allure-playwright": "^3.2.2"
  },
  "dependencies": {
    "@faker-js/faker": "^9.8.0"
  }
}
